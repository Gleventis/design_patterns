## Dependency Injection

Design pattern for passing objects(dependencies) to a class(or function).

These can be passed via:
- a constructor
- a public property or field
- a public setter

Very useful for decoupling classes from their dependencies.
